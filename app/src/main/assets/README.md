Coloca aqui estos archivos antes de ejecutar la app:

- `regula.license`
- `Regula/db.dat` opcional

Si no incluyes `Regula/db.dat`, la app puede intentar descargar la base con `prepareDatabase("Full")` antes de inicializar el SDK.
